# Thesis utilities

Personal utility functions (work in progress).

# Installation
In a terminal upgrade your installer tools via:
```bash
python -m pip install --upgrade pip setuptools wheel
```
Then install from inside the outer thesis_utils folder:
```
python -m pip install -e .
````
