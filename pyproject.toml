[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "thesis-utils"
version = "0.1.0"
description = "Private utility functions for thesis data analysis"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]